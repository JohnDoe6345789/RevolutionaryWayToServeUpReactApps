# bootstrap/controllers

Controller helpers that coordinate the lifecycle of bootstrap, including orchestrating config loading, services, and rendering zones.

## Documents

- [`base-controller.md`](base-controller.md) – Describes the base controller helpers that route config, logging, and rendering actions.
- [`bootstrapper.md`](bootstrapper.md) – Explains how the bootstrapper wires the controller lifecycle together with CDN/local services.
