# bootstrap/initializers/path-utils

This directory covers the utilities that normalize local module paths and aliases used by the loader.

## Documents

- [`local-paths.md`](local-paths.md) – Lists helpers like `isLocalModule`, `normalizeDir`, and alias key generation.
