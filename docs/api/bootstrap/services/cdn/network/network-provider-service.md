# Module: `bootstrap/services/cdn/network/network-provider-service.js`

## Overview

- **Purpose:** Normalizes provider fallbacks, proxy modes, and alias maps before the loader resolves CDN URLs.

## Globals
- `BaseService`
- `NetworkProviderService`
## Functions / Classes

- `NetworkProviderService` — class that calibrates proxies, fallback providers, aliases, and exposes helpers such as `collectBases`, `normalizeProviderBase`, and `resolveProvider`.

## Related docs

- [Bootstrap CDN network README](../README.md)
