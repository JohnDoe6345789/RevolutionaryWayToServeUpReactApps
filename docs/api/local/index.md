# Local path helpers

This folder covers the filesystem-style helpers that resolve relative imports and generate candidate paths for the loader.

## Documents

- [`local-paths.md`](local-paths.md) – `isLocalModule`, `normalizeDir`, `resolveLocalModuleBase`, and related utilities described.

Use them when implementing custom local module loaders or when preloading modules from source strings.
