{
  "name": "revolutionary-way-to-serve-up-react-apps",
  "version": "1.0.0",
  "description": "Monorepo for Revolutionary Way to Serve Up React Apps",
  "private": true,
  "scripts": {
    "lint": "bun run lint.ts",
    "clean": "bun run scripts/clean.ts",
    "clean:node-modules": "bun run scripts/clean.ts --targets=node_modules",
    "clean:dist": "bun run scripts/clean.ts --targets=dist,build,out",
    "clean:cache": "bun run scripts/clean.ts --targets=.cache,.next,coverage",
    "clean:all": "bun run scripts/clean.ts --targets=node_modules,dist,build,out,.cache,.next,coverage"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [
    "monorepo",
    "react",
    "nextjs",
    "codegen",
    "linting"
  ],
  "author": "Revolutionary Codegen Team",
  "license": "MIT"
}
